class ModalContentTemplate extends ModalContent{
    get headerElement(){
        // return a html element that can be appended to the modal
    }

    get bodyElement(){
        // return a html element that can be appended to the modal
    }

    get footerElement(){
        // return a html element that can be appended to the modal
    }

    constructor(optParams) {
        super();

        /*
        let params = new Map()
        params.set("key1", "value1")
        params.set("key2, value2")
        super(params);
        */

    }
}